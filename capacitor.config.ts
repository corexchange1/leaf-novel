import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lan.leafnovel.reader',
  appName: 'Leaf Novel',
  webDir: 'dist',
  android: {
    backgroundColor: '#EDEFF0',
  },
};

export default config;
